# FIREWOLF UI v2 タスク管理

## タスク一覧

### 実装待ち

#### UI改善タスク
- [ ] ToggleSliderコンポーネントの実装と適用
  - ToggleSliderコンポーネントを新規作成
  - ON/OFF状態のデザインとアニメーション実装
  - 村作成画面のcheckboxをToggleSliderに置き換え
  - アクセシビリティ対応（キーボード操作、aria属性）

- [ ] キャラチップ一覧・詳細画面でのCharacterImage使用
  - キャラチップ一覧画面でCharacterImageコンポーネントを使用
  - キャラチップ詳細画面でCharacterImageコンポーネントを使用
  - 既存のインライン画像表示をコンポーネント化

#### 村作成機能
- [ ] 編成セクションの実装
  - 役職構成セクションをキャラチップセクションの後に移動
  - セクション名を「編成」に変更
  - 最小人数・定員の入力フィールドを実装（初期値: 10, 16）
  - 役欠け設定の入力フィールドを実装（初期値: なし）
  - 編成設定時の注意事項を表示（info box形式）
  - ルールページへのリンクを追加（target=_blank）

### 実装中

### 完了
- [x] ダミーキャラ機能の改善
  - ダミーキャラ select の右側に「画像から選ぶ」ボタンを配置
  - 「画像から選ぶ」をクリックしたらダミーキャラを選ぶモーダルを表示
  - 画像とキャラ名の card を並べて、ダミーキャラを選択可能にする
  - ダミーキャラを選択したら、ダミーキャラ名にそのキャラクター名を、ダミーキャラ略称に略称を自動で上書き
  - プロローグメッセージセクションを「ダミーキャラ発言」セクションに変更
  - プロローグ発言、1日目発言の2つを入力可能にする
  - 「1日目発言のみ、村作成後も変更できます」のinfo文を表示

- [x] ルールページ能力欄の拡充
  - v1のルールページから能力欄の内容をコピー
  - 現在不足している能力説明を追加（噛み、賢者、検死官、猫又、パン屋、共鳴、狂人系、背徳者、求愛、探偵、サイコ等）
  - 役職一覧からのアンカーリンク対応を確認・修正（各能力にid属性追加）

### 完了
- [x] ルールページの役職一覧機能の実装
  - API `/skill/list` から役職一覧を取得
  - テーブル形式で役職情報を表示
  - 役職名クリックでdescriptionをアコーディオン展開
  - 能力クリックで該当セクションへのアンカースクロール
  - 人狼陣営関連項目の赤文字表示
  - レスポンシブ対応
  - 型定義の適切な分離（API自動生成型の直接使用を禁止）

### 完了

#### UI改善タスク
- [x] 村カード全体をリンクにする
  - VillageCardコンポーネントを修正し、カード全体をクリッカブルに変更
  - ホバー時の視覚的フィードバックを追加
  - アクセシビリティを考慮したフォーカス状態の実装

#### 村作成機能
- [x] 時間セクションの改善
  - 開始日時と沈黙時間を「時間」セクションにまとめる
  - 「1日の長さは24時間固定です」をinfoとして表示
  - 開始日時は7日後0時を初期設定にする
  - 沈黙時間は0を初期設定にする

- [x] キャラチップセクションの実装
  - API `/charachip/list` でキャラチップ一覧を取得し、複数選択可能にする
  - 1つ目を初期選択
  - ダミーキャラをキャラ一覧から選択可能にする
  - API `/charas` で選択したキャラチップidを指定してキャラ一覧を取得
  - 一人目を初期選択
  - 「画像から選ぶ」ボタンでモーダル表示、キャラ画像とともに選択可能（TODO実装済み）

- [x] 投げ銭モーダルの実装
  - v1のmodal-kampa.vueから内容を移行
  - 投げ銭用のモーダルコンポーネントを作成
  - フッターの投げ銭リンクからモーダルを開く

- [x] PC版ヘッダーのリンク削除
  - デスクトップ版ヘッダーから「村一覧」「ルール」「村作成」「設定」リンクを削除
  - よりシンプルなヘッダーデザインに変更

## 優先順位

1. **最優先**: bugs.mdに記載されたバグ修正
2. **高**: features.mdに記載された新機能要望
3. **中**: リファクタリングやパフォーマンス改善
4. **低**: ドキュメント更新

## 注意事項

- 各タスクは適切な粒度でコミットする
- 実装後は必ず lint、format、type-check を実行
- API の型は自動生成されたものを使用
- ドキュメントは実装と並行して更新
- v1 との互換性を意識（URL 構造、API 形式等）