# FIREWOLF UI v2 タスク管理

## タスク一覧

### 村画面動作確認・修正フェーズ

#### 現在のステータス
- ✅ 村画面の初期実装が完了（全フェーズ実装済み）
- 🔄 動作確認・品質向上フェーズに移行

#### 動作確認と修正要望タスク

##### UI/UX 改善タスク

**📋 タスク**: 村画面でのページタイトル動的設定機能を実装
- **内容**: 村画面で `FIREWOLF | {村名}` 形式のタイトル表示
- **実装方法**: 
  - `src/app/(public)/village/[id]/page.tsx` に `generateMetadata` 関数を追加
  - village API から村情報を取得し動的にタイトルを設定
  - エラー時は `FIREWOLF` をデフォルト表示
- **ステータス**: 🆕 未着手
- **優先度**: 中
- **推定工数**: 0.5日

**📋 タスク**: 固定メニュー部分のダークテーマ実装
- **内容**: サイドバーとメインコンテンツの固定メニュー部分に黒背景 `#363636` を適用
- **実装対象**:
  - サイドバーのVillageInfoコンポーネント（村情報表示部分）
  - メインコンテンツのTopFixedMenu・BottomFixedMenuコンポーネント
- **実装方法**:
  - Tailwind CSSのカスタムクラス `.dark-fixed-menu` を定義
  - 対象コンポーネントに適用、テキストとボーダーの色も調整
  - アクセシビリティ（コントラスト比）を維持
- **ステータス**: 🆕 未着手
- **優先度**: 高（features.mdからの要望）
- **推定工数**: 0.5日

## 優先順位

1. **最優先**: bugs.md に記載されたバグ修正
2. **高**: features.md に記載された新機能要望
3. **中**: リファクタリングやパフォーマンス改善
4. **低**: ドキュメント更新

## 注意事項

### 開発プロセス

- 各タスクは適切な粒度でコミットする
- 実装後は必ず lint、format、type-check を実行
- API の型は自動生成されたものを使用
- ドキュメントは実装と並行して更新
- v1 との互換性を意識（URL 構造、API 形式等）

### 状態管理の重要原則

- **フィルタ・日付状態は永続化しない**（別タブ独立性のため）
- **ユーザー設定・メモのみ Cookie 保存**（永続化が必要なもののみ）
- **各タブはデフォルト状態から開始**（最新日・全フィルタ ON）
- **URL パラメータでの初期化対応**（個人抽出・日付指定）

### パフォーマンス要件

- **リアルタイム更新間隔**：残り時間 1 秒、発言データ 30 秒、村情報 60 秒
- **サーバー負荷軽減**を最優先に設計
- **仮想化・メモ化**を適切に適用

### UI/UX 要件

- **システムメッセージの正確な色設定**（message.md 準拠）
- **アクセシビリティ対応**（WCAG 2.1 AA 準拠）
- **別タブでの柔軟な利用**を前提とした設計
